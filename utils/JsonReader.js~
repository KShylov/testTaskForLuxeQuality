import fs from 'fs';
import path from 'path';

class JsonReader {
    constructor(fileName) {
        this.fileName = fileName;
        const __dirname = process.cwd() + '/utils';
        this.filePath = path.resolve(__dirname, 'data',  this.fileName);
    }

    readJson() {
        try {
            const data = fs.readFileSync(this.filePath, 'utf-8');
            return JSON.parse(data);
        } catch (error) {
            console.error(`Error reading JSON file at ${this.filePath}: ${error.message}`);
            return null;
        }
    }
}

export default JsonReader;
